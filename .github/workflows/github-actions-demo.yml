name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions on ${{ github.ref }}

on: 
  push:
    branches:
    - main
    - develop

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Probar si el branch es el correcto
      if: github.ref == 'refs/heads/main'
      run: |
        current_time_cdmx=$(TZ=America/Mexico_City date +'%u %H')
        echo $current_time_cdmx
        # day_of_week=$(echo $current_time_cdmx | cut -d' ' -f1)
        # echo "Hora y día en CDMX: $current_time_cdmx"

        # hour=$(echo $current_time_cdmx | cut -d' ' -f2)
        # if [ "$day_of_week" -eq 1 ] && [ "$hour" -eq 9 ]; then
        #   echo "Es lunes 9 AM CDMX, ejecutando job"
        #   echo "run_job=true" >> $GITHUB_OUTPUT
        # else
        #   echo "No es lunes 9 AM CDMX, cancelando job"
        #   echo "run_job=false" >> $GITHUB_OUTPUT
        # fi

        # echo "❌ Este flujo no debería correr en 'main'. Abortando..."
        # exit 1

    - name: Accion 02
      run: echo "accion 02 a"


      # - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      # - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      # - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
      # - name: Check out repository code
      #   uses: actions/checkout@v4
      # - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      # - run: echo "🖥️ The workflow is now ready to test your code on the runner."
      # - name: List files in the repository
      #   run: |
      #     ls ${{ github.workspace }}
      # - run: echo "🍏 This job's status is ${{ job.status }}."

